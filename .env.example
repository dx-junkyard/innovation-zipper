# =============================================================================
# Hybrid LLM Configuration
# =============================================================================
# This system supports both Local LLM (Ollama) and Cloud API (OpenAI) providers.
# You can use both simultaneously and route tasks to the optimal provider.

# --- OpenAI API (Optional) ---
# Set your API key to enable OpenAI provider
#OPENAI_API_KEY=your_openai_api_key

# --- Local LLM (Ollama) ---
# Default: http://host.docker.internal:11434 (for Docker)
# Change to http://localhost:11434 if running outside Docker
#AI_URL=http://host.docker.internal:11434

# --- Cloud LLM Model Settings (OpenAI) ---
#CLOUD_MODEL_FAST=gpt-4o-mini
#CLOUD_MODEL_SMART=gpt-4o
#CLOUD_EMBEDDING_MODEL=text-embedding-3-small
#CLOUD_EMBEDDING_DIMENSION=1536

# --- Local LLM Model Settings (Ollama) ---
#LOCAL_MODEL_FAST=llama3.2
#LOCAL_MODEL_SMART=llama3.2
#LOCAL_EMBEDDING_MODEL=mxbai-embed-large
#LOCAL_EMBEDDING_DIMENSION=1024

# --- Legacy Settings (Deprecated - use provider-specific settings above) ---
#LLM_MODEL=gpt-4o-mini
#EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# =============================================================================
# LINE Integration
# =============================================================================
LINE_CHANNEL_ID=your_line_channel_id
LINE_CHANNEL_SECRET=your_line_channel_secret
LINE_REDIRECT_URI=http://localhost:8080

# =============================================================================
# S3 / MinIO Configuration
# =============================================================================
S3_ENDPOINT_URL=http://minio:9000
S3_PUBLIC_ENDPOINT_URL=http://localhost:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadminpassword
S3_BUCKET_NAME=user-files
S3_REGION_NAME=us-east-1
S3_USE_SSL=False

# =============================================================================
# Database Configuration
# =============================================================================
#DB_HOST=db
#DB_USER=me
#DB_PASSWORD=me
#DB_NAME=mydb
#DB_PORT=3306

# =============================================================================
# Vector Database (Qdrant)
# =============================================================================
#QDRANT_HOST=localhost
#QDRANT_PORT=6333

# =============================================================================
# Task Queue (Celery/Redis)
# =============================================================================
#CELERY_BROKER_URL=redis://localhost:6379/0

# =============================================================================
# Wikipedia Import
# =============================================================================
#WIKIPEDIA_UPLOAD_DIR=/tmp/wikipedia_dumps
